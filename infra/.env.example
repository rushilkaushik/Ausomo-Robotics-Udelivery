# Deployment-level infra variables go here.
# Runtime env files live beside the code that consumes them:
#
# - apps/web/.env.example -> apps/web/.env.local
# - backend/.env.example  -> backend/.env
# - robot/.env.example    -> robot/.env
#
# Keep service-role keys out of frontend env files.
